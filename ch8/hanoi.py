def hanoi(n, start=None, spare=None, finish=None):
    """Move n disks from the first tower to the third."""

    # if start is None and spare is None and finish is None:
    #     start = range(n, 0, -1)
    #     spare = []
    #     finish = []

    # while len(finish) != n:
    #     if len(spare) == 0:
    #         spare.append(start.pop())

    #     if len(finish) == 0:
    #         finish.append(start.pop())

    print "Supposedly this can be done in like 5 lines..."






    